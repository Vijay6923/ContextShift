# Legacy pre-refactor implementations, kept only as characterization
# fixtures -- see tests/test_*_characterization.py, which assert the
# equivalent contextshift/ code matches this package's behavior. Not
# imported by anything in contextshift/ or examples/.
