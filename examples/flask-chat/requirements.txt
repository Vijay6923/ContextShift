# Installs contextshift itself from this checkout, editable. app.py
# imports it directly and is no longer next to contextshift/ in the
# directory tree (see ../../README.md's Project Structure section), so
# it must be installed rather than resolved via Python's implicit
# script-directory sys.path entry the way it could when this app lived
# at the repository root.
-e ../..
flask
flask-cors
flask-sqlalchemy
python-dotenv
requests
PyPDF2
Pillow
google-genai
gunicorn
