# Installs contextshift itself (editable) plus pytest (see
# pyproject.toml's [project.optional-dependencies].dev).
# pyproject.toml's core [project.dependencies] already covers
# everything examples/flask-chat/ needs (Flask, Flask-Cors,
# Flask-SQLAlchemy, python-dotenv, requests, PyPDF2, Pillow,
# google-genai) -- this file exists only so
# `pip install -r requirements-dev.txt` remains the one command that
# sets up a full contributor environment, library and example app alike.
-e .[dev]
