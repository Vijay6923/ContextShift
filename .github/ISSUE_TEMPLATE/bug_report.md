---
name: Bug report
about: Something in contextshift (or the example app) doesn't behave as documented
title: ""
labels: bug
assignees: ""
---

## What happened

A clear description of the incorrect behavior.

## Minimal reproduction

A short, self-contained code sample — ideally a `Message` list, a
`TokenBudget`, and the strategy/tokenizer/provider involved:

```python
from contextshift import ContextManager
# ...
```

## Expected behavior

What you expected to happen instead, and why (a docstring, an ADR, or
a benchmark result that this contradicts, if applicable).

## Environment

- `contextshift` version:
- Python version:
- OS:
- Relevant optional dependencies installed (`tiktoken`, `anthropic`), if any:

## Additional context

Anything else relevant — stack trace, related issue, whether this
reproduces on `main`.
